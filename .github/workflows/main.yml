permissions:
  contents: read
  actions: write

name: CD
run-name: CD

on:
  workflow_run:
    workflows: ["CI"]
    types:
      - completed

jobs:
  on-success:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Workflow triggered'
