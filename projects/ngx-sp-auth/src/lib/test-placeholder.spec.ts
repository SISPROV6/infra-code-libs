// Lucas, criei esse arquivo e removi os outros dois pois eles estavam com erros nos testes, este aqui é apenas para que tenha um teste que o Karma execute.
// Sem ele, a publicação do ngx-sp-infra fica cagada
describe('Arquivo de placeholder para testes unitários do ngx-sp-auth', () => {

  it('should pass', () => {
    expect(true).toBeTrue();
  });

});
